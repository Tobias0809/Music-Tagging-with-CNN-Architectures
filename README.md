# Music-Tagging-with-CNN-Architectures
This project addresses multi-label music genre classification, where each track may be associated with several genres and the label distribution is highly imbalanced. The goal is to build a model that can reliably assign the most relevant genre labels to each track.
